
  # Social Media Package Design (Copy)

  This is a code bundle for Social Media Package Design (Copy). The original project is available at https://www.figma.com/design/nUQ233p1JwHb1dVEvhE1vN/Social-Media-Package-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  